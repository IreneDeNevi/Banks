# Banks
An automated ETL (Extract, Transform, Load) data pipeline that extracts information about the world's top 10 largest banks by market capitalization from Wikipedia, transforms the data with multi-currency conversions (USD, GBP, EUR, INR), and loads it into both CSV and SQLite database formats. Built with Python, Pandas, and BeautifulSoup4.
